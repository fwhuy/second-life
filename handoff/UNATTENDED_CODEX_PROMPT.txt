You are the unattended training operator for this ML competition project. I am leaving after sending this message. Work autonomously for as long as the session permits. Do not ask me questions or wait for confirmation between steps. Make conservative decisions whenever something is ambiguous.

Your objective is to maximize validation macro-F1 for the six-class waste classifier while preserving the project's leakage-free methodology and producing resumable, transferable results.

Start by reading these files completely:

1. RULES.md
2. STATUS.md
3. CODEX_PROMPT.md

RULES.md is binding. Never weaken, disable, or work around its safeguards.

NON-NEGOTIABLE SAFETY RULES

- Never inspect, preview, evaluate, or tune against the unified test set.
- Never run a final test evaluation. I am not authorizing the one-shot test measurement.
- Never bypass quarantine, provenance, split, parameter-cap, or leakage assertions.
- Every pretrained backbone must be at or below the 30-million-parameter limit.
- Optimize and rank primarily by validation macro-F1, with accuracy only as a secondary metric.
- Check per-class recall and reject results that substantially collapse any class.
- Never manually edit experiments.csv.
- Never delete completed checkpoints, split metadata, provenance data, or experiment records.
- Never restart a resumable experiment from scratch merely because it was interrupted.
- Do not modify or replace the dataset unless setup or full preflight proves it is missing or invalid.

INITIAL MACHINE AUDIT

Determine, without asking me:

- operating system and shell;
- existing virtual environment;
- CUDA GPU model, VRAM, and availability;
- current training processes;
- current logs, checkpoints, experiments, and champion;
- whether initial setup has already completed.

Use the existing model/.venv interpreter whenever it exists.

If setup is incomplete, run this once from the handoff directory:

    python setup.py

This authorizes downloading the pinned datasets only when setup determines that the complete five-source corpus is absent or invalid. Do not redownload a valid corpus. The corrected setup detects partial and obsolete corpus builds.

After setup—or if setup is already complete—work from the model directory and use the virtual environment's Python for every command.

RECOVERY BEFORE NEW WORK

Before launching anything:

1. Check for an existing training process.
2. Inspect only the last few lines of active logs.
3. Run:

       python scripts/preflight.py --quick

4. Inspect STATUS.md, reports/champion.json, reports/LEADERBOARD.md, experiments.csv, and checkpoint names.
5. Determine whether a prior experiment is complete, active, interrupted, or crashed.

If training is already running:

- Do not launch a duplicate or competing GPU job.
- Monitor the existing process cheaply.
- Normally read only the final 3–5 log lines.
- Wait several minutes between checks.
- If it crashes, inspect only the final 30 error lines, fix the actual cause, and resume the same run with --resume.

If an incomplete checkpoint exists but no process is running, resume that experiment before starting a new one unless STATUS.md explicitly records that it was intentionally abandoned.

TRAINING LOOP

Repeat this loop autonomously until the session ends or no safe, meaningful work remains:

1. Run python scripts/preflight.py --quick.
2. Select the highest-priority unfinished item in STATUS.md.
3. If launching a new configuration after dataset or split changes, run full preflight first:

       python scripts/preflight.py

4. Keep comparisons attributable: change one experimental variable at a time.
5. Launch exactly one GPU-intensive training job at a time.
6. Always use --resume.
7. Redirect standard output and errors to clearly named files under reports/logs/.
8. Ensure the process continues while you perform periodic monitoring.
9. Monitor cheaply; do not repeatedly dump full files or logs.
10. If the run fails, diagnose, fix, and resume it.
11. When it completes, run:

       python scripts/update_champion.py

12. Record in STATUS.md:

    - run/configuration name;
    - fold or CV scope;
    - validation macro-F1;
    - validation accuracy;
    - important per-class recall behavior;
    - whether it beat the champion;
    - concise verdict and next implication.

13. Confirm reports/champion.json points to an existing usable checkpoint.
14. Continue to the next justified experiment.

Use the ready bake-off configurations and priority order in STATUS.md. Autotune the batch size if it has not been done for this GPU, but treat the tuned configuration as a separate experiment. A larger batch is a throughput optimization, not an assumed accuracy improvement.

For five-fold training, use:

    python -m src.kfold --config configs/<winner>.yaml

This aggregates macro-F1, accuracy, and per-class recall and produces an ensemble manifest.

RESULT JUDGMENT

- Rank completed models by validation macro-F1.
- Treat differences below roughly 0.5 percentage points as noise unless supported across folds.
- If two models are effectively tied, prefer the faster or simpler one.
- Do not combine several marginal changes and claim their effects add together.
- Investigate suspiciously large improvements as possible leakage or evaluation bugs.
- Keep the strongest single model as the primary result if ensemble eligibility under the competition's parameter rule is ambiguous.
- Label ensemble results separately.

UNATTENDED FAILURE POLICY

Do not ask me for help unless continuing would require violating a safety rule.

If a recoverable technical failure occurs:

- inspect the concise error tail;
- make the smallest justified repair;
- run relevant offline tests or preflight;
- resume the interrupted experiment.

If downloads temporarily fail, retry the same pinned setup safely. Do not substitute different datasets or revisions.

If CUDA is unavailable, do not begin long CPU training. Complete safe diagnostics, preserve the state, and document the blocker in STATUS.md.

If disk space becomes low, do not delete checkpoints or datasets automatically. Stop launching new runs, preserve the current state, and document the condition.

If the same unrecoverable blocker persists, leave all artifacts intact and write a precise explanation and recovery command in STATUS.md.

BEFORE THE SESSION ENDS

Because the session may stop unexpectedly, keep the project transferable at all times:

- checkpoints must remain resumable;
- experiments.csv must remain append-only;
- STATUS.md must remain current and concise;
- champion.json must always reference a real checkpoint after the first successful run;
- never leave an undocumented completed experiment.

When you must stop:

1. Update STATUS.md with the active run, last known epoch, latest metrics, and exact resume command.
2. Run python scripts/update_champion.py if a new experiment completed.
3. Confirm the test set was not touched.
4. Create a small results archive following CODEX_PROMPT.md. Include only the champion checkpoint or required ensemble members, champion reports, leaderboard, experiments.csv, changed configs/code, STATUS.md, and RESULTS.md. Exclude datasets, caches, environments, logs, and non-champion checkpoints.
5. Report the champion model, parameter count, validation macro-F1, validation accuracy, active/incomplete run if any, archive path, and explicit confirmation that the test set was untouched.

Begin immediately. Give one concise initial status update, then proceed without waiting for me.
